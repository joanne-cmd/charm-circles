version: 8

apps:
    $00: a/${app_id}/${app_vk}

private_inputs:
    $00: "${in_utxo_0}"

ins:
    - utxo_id: ${in_utxo_0}
      charms: {}

outs:
    - address: ${circle_address}
      charms:
          $00:
              data: ${circle_state_serialized}